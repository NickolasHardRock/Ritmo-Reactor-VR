node_modules/
dist/
.env
.env.local
.vercel
*.log
npm-debug.log*

# Modelos-fonte NÃO versionados: são pesados e não são o que o jogo carrega.
# O que vai para o repositório é o resultado do ferramentas/otimizar-cenario.mjs,
# em frontend/public/modelos/. Guardem os originais fora do Git (Drive da equipe).
ferramentas/fonte/
*.blend
*.fbx
sci-fi_lab.glb
drum_kit.glb

.DS_Store
Thumbs.db
frontend/public/libs/

# Modelos de trabalho (pesados, não otimizados)
frontend/public/modelos/cenario2.glb
frontend/public/modelos/flame_test*.glb
frontend/public/modelos/lab_old.glb
frontend/public/modelos/lab_old2.glb
frontend/public/modelos/world_of_metal.glb
# Fonte do bicho, direto do Meshy: 7,3 MB e 116 mil triangulos.
# O que vai para o repositorio e o bicho.glb dizimado, de 0,25 MB.
frontend/public/modelos/Meshy_*.glb

# Sessões Claude e lixo temporário
.claude/
_to_delete/

# Faixa de audio da fase de ritmo: uso didatico em sala, NAO distribuir.
# Cada um guarda a copia local; commitar aqui seria publicar.
frontend/public/sounds/som.mp3
# A carta dela tambem fica fora: descreve o arranjo nota por nota.
frontend/public/cartas/money*.json

# Diesel13 'Colour Me Red' ENTRA no repositorio, por decisao do Diego.
# O Readme do pacote diz "educational purposes only" e veta uso comercial
# sem permissao -- e este trabalho e academico e nao comercial. O que sobe
# e a MIXAGEM (obra derivada), que e para isso que a biblioteca existe;
# a multipista crua de 478 MB nao sobe. O credito aparece na abertura e no
# resultado, e o robots.txt mantem o deploy fora dos buscadores.

# Backups de cenario e intermediarios do otimizador (12 MB cada)
frontend/public/modelos/lab_antes_*.glb
frontend/public/modelos/lab_com_*.glb
frontend/public/modelos/world_of_metal_*.glb

# Saidas de sessao do assistente
Claude outputs/

# As tres variantes de textura da bateria em KTX2 (uastc, misto, etc1s), que
# a chave `?kit=` do config.js seleciona. Sao 24 MB de arquivo DESCARTAVEL:
# existem para comparar qualidade no Quest 3 e, escolhida uma, ela vira o
# `bateria_pratos.glb` e as outras duas somem junto com a chave. Versiona-las
# deixaria os 24 MB no historico para sempre, inclusive das duas perdedoras.
# Quem precisar testar pede os arquivos a equipe.
frontend/public/modelos/bateria_uastc.glb
frontend/public/modelos/bateria_misto.glb
frontend/public/modelos/bateria_etc1s.glb
