{
  "name": "ritmo-reactor-vr",
  "private": true,
  "version": "0.1.0",
  "description": "Drumfall — jogo WebXR de ritmo. ADS Senac Joinville.",
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "dev:api": "npm run dev --workspace backend",
    "build": "npm run build --workspace frontend",
    "preview": "npm run preview --workspace frontend",
    "test": "npm run build && node ferramentas/teste-jogo.mjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "meshoptimizer": "^1.2.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@gltf-transform/core": "^4.5.0",
    "@gltf-transform/extensions": "^4.5.0",
    "@gltf-transform/functions": "^4.5.0",
    "playwright": "^1.49.1",
    "sharp": "^0.35.4"
  }
}
